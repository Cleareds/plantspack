{
  "permissions": {
    "allow": [
      "Bash(eas build:list --platform ios --limit 5 --non-interactive)",
      "Bash(grep -E \"^[0-9a-f]{7,}|buildNumber|versionCode\")",
      "Bash(sips -g pixelWidth -g pixelHeight -g hasAlpha assets/icon.png)"
    ]
  }
}
