// Twitter card uses the same composition as the OpenGraph image.
// Re-exporting from the OG file keeps a single source of truth.
export { default, runtime, alt, size, contentType } from './opengraph-image'
